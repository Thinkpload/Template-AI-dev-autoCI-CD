мне нужны реальные технологии для внедрения в репозиторий. Чтобы человек, когда пуллит и запускает некий SETUP или ONBOARDING устанавливал себе все необходимые фичи такие как методологию BMAD, CI xrthtp github actions с автоматическим созданием issues и так далее. Вот такие технологии/репозитории мне нужны. Чтобы сделать свой шаблон из всего лучшего, некий максимально преднастроенный проект, чтобы не приходилось для каждого Saas и не только делать все по новой.

Есть задача. Мне нужно есть желание создать некий темплейт, шаблон для разработки SaaS проектов. Это может быть SaaS, это может быть какой-то другой проект разработки, может быть просто какой-то продуктовые исследования, может быть какое-то преложение, может быть сервис, который будет разрабатываться в том числе с помощью ИИ. Это практически обязательно. То есть тут нужно, во-первых, AIDriven Development. Например, BMAD и, например, get-Shit-Done, GSD. Также должно быть CI/CD преднастройки, Github actions, тесты, QA и шаблоны, типа Lint, тестирование. Это уже частично реализовано. В этом черновике проекта можешь посмотреть. Также там есть исследование по тем элементам, которые еще туда можно добавить 

C:\Users\gllex\!DEV_PROJECTS_2026\!Template_BMAD+auto-CI-CD\research\RESEARCH-template-technologies.md , 

но относиться к нему. Нужно скептически все проверить и еще исследовать вопрос. Также проверка качества кода, там привязан Sonar Cloud. И сделаем workflow для автоматического бакфикса, чтобы еще создавались на GitHub Issues автоматически. И, по возможности, автоматически решались  запуском простой команды ИИ. В общем, нужно создать некий визард wizard, некий помощник, мастер установки проекта, которые бы ты запускал, и он на основании твоих потребностей подключал бы и устанавливал те модули, которые тебе нужны. То есть если ты юзаешь методологию Bmad, выбирать BMAD, потом выполнять установку BMAD через NPM, например. Из известного публичного репозитория. Если ты выбрал GSD, get Shit Done, тогда выбирать другое (или использовать оба, тоже можно.). Он выбирает это и запускает его установку. И также по остальным модулям. Главное сделать решение максимально универсальным, так как это будет публичный репозиторий, публичный темплейт на GitHub. Важно учить эти особенности, так как запросы у разработчиков, да и вообще в принципе у людей будут разные. Кому-то не обязательно будет разрабатывать приложение, но в основном я хочу делать для установки и удобной работы с разработки новых продуктов. Так как сейчас ускоряется разработка, и необходимо быстро уметь настраивать все эти окружения, переменные, агенты, скиллы, воркфлоу и так далее. Нужно понять, какую IDE, точнее даже какую агентик систему использует. То есть это может быть клод-код, это может быть антигравити, может быть вс код со своими extentions. Это тоже нужно предусмотреть, и под это наложить вопросы или в шаблон, включить все необходимое в скрипт, чтобы устаналивались последние версии с публичных репозиториев. Вот такое исследование необходимо провести, посмотреть, что уже сделано сейчас, у конкурентов, и дальше пройти по процессу его полного создания этого некого шаблона шаблона. То есть продукт шаблон получается. 

missing
no skills dir
no agents skills dir