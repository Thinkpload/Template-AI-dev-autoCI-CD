---
phase: 05-packaging-and-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - .github/workflows/dependabot-automerge.yml
  - wizard/src/index.ts
  - wizard/package.json
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "Running `npm run build` in wizard/ produces dist/index.cjs and the binary does not crash on valid Node 20+"
    - "Running `node -e` to simulate Node 18 against dist/index.cjs prints the user-friendly version error and exits 1"
    - ".github/dependabot.yml exists and configures weekly npm (directory: /wizard) and github-actions updates grouped by minor+patch"
    - ".github/workflows/dependabot-automerge.yml exists and auto-merges Dependabot PRs when update-type is version-update:semver-patch"
    - "`npm publish` run locally without CI is blocked by the prepublishOnly gate (tests + build must pass first)"
  artifacts:
    - path: ".github/dependabot.yml"
      provides: "Weekly Dependabot PRs for npm and GitHub Actions ecosystems"
      contains: "package-ecosystem"
    - path: ".github/workflows/dependabot-automerge.yml"
      provides: "Automatic merge of patch-only Dependabot PRs"
      contains: "dependabot/fetch-metadata"
    - path: "wizard/src/index.ts"
      provides: "Node version guard before any imports"
      contains: "process.versions.node"
    - path: "wizard/package.json"
      provides: "prepublishOnly safety net"
      contains: "prepublishOnly"
  key_links:
    - from: "wizard/src/index.ts"
      to: "process.exit(1)"
      via: "Node version check before imports"
      pattern: "process\\.versions\\.node"
    - from: ".github/workflows/dependabot-automerge.yml"
      to: "dependabot/fetch-metadata@v2"
      via: "update-type == version-update:semver-patch condition"
      pattern: "fetch-metadata"
---

<objective>
Create the dependency maintenance infrastructure (SEC-01) and add the Node version guard to the wizard binary.

Purpose: Satisfy the only remaining v1 requirement (SEC-01) and protect users running old Node versions from cryptic errors.
Output: `.github/dependabot.yml`, `.github/workflows/dependabot-automerge.yml`, updated `wizard/src/index.ts` with version guard, updated `wizard/package.json` with prepublishOnly script.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-and-maintenance/05-CONTEXT.md
@.planning/phases/05-packaging-and-maintenance/05-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts for the executor. No codebase exploration needed. -->

From wizard/src/index.ts (current state — lines 1-34):
```typescript
// CJS-safe package.json version read (no import.meta.url in CJS)
const { version } = require('../package.json') as { version: string };

import { runWizard } from './wizard.js';
import { buildInitialConfig, readConfig, writeConfig } from './config.js';
import { runInstaller } from './installer.js';
// ... rest of main()
```

From wizard/package.json (current scripts section):
```json
{
  "scripts": {
    "build": "tsup",
    "test": "vitest run --reporter=verbose",
    "test:coverage": "vitest run --coverage"
  }
}
```

Node guard must be placed at line 1, BEFORE the `require('../package.json')` call on the current line 7. The guard uses only built-in `process` — no require/import needed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Node version guard in index.ts and prepublishOnly in package.json</name>
  <files>wizard/src/index.ts, wizard/package.json</files>
  <action>
    **wizard/src/index.ts** — Insert the Node version guard as the very first executable code, before ALL existing content (including the `require('../package.json')` line):

    ```typescript
    // Node version guard — must be first executable code (before any require/import)
    // tsup CJS output preserves source order, so this runs before all require() calls
    const _nodeVer = process.versions.node;
    const _nodeMajor = parseInt(_nodeVer.split('.')[0], 10);
    if (_nodeMajor < 20) {
      process.stderr.write(
        `Error: create-ai-template requires Node.js 20 or higher. You are running v${_nodeVer}. Please upgrade: https://nodejs.org\n`
      );
      process.exit(1);
    }
    ```

    Place this block at the top of the file, followed by the existing content unchanged. The file structure after edit:
    1. Node version guard block (lines 1-9)
    2. JSDoc comment (existing)
    3. `const { version } = require(...)` (existing)
    4. imports (existing)
    5. rest of main() (existing)

    **wizard/package.json** — Add `prepublishOnly` script to the `scripts` object:
    ```json
    "prepublishOnly": "npm test && npm run build"
    ```
    No other changes to package.json. The `engines`, `bin`, `type` fields remain unchanged.
  </action>
  <verify>
    <automated>cd wizard && npm run build && echo "build OK" && node -e "Object.defineProperty(process.versions, 'node', { value: '18.0.0' }); require('./dist/index.cjs')" 2>&1 | grep -q "requires Node.js 20" && echo "version guard OK"</automated>
  </verify>
  <done>
    - `wizard/dist/index.cjs` builds successfully with `npm run build`
    - Simulating Node 18 against the built binary prints `Error: create-ai-template requires Node.js 20 or higher` to stderr and exits 1
    - `wizard/package.json` contains `"prepublishOnly": "npm test && npm run build"` in scripts
    - Existing tests still pass: `cd wizard && npm test` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Dependabot config and automerge workflow (SEC-01)</name>
  <files>.github/dependabot.yml, .github/workflows/dependabot-automerge.yml</files>
  <action>
    **Create `.github/dependabot.yml`:**

    ```yaml
    # Dependabot version update configuration — SEC-01
    # Weekly PRs for npm (wizard/) and GitHub Actions, grouped by minor+patch.
    # Major updates appear as individual PRs (default behavior when not grouped).
    version: 2
    updates:
      - package-ecosystem: 'npm'
        directory: '/wizard'
        schedule:
          interval: 'weekly'
          day: 'monday'
        groups:
          npm-minor-patch:
            applies-to: version-updates
            update-types:
              - 'minor'
              - 'patch'
        labels:
          - 'dependencies'

      - package-ecosystem: 'github-actions'
        directory: '/'
        schedule:
          interval: 'weekly'
          day: 'monday'
        groups:
          actions-minor-patch:
            applies-to: version-updates
            update-types:
              - 'minor'
              - 'patch'
        labels:
          - 'dependencies'
    ```

    Note: `directory: '/wizard'` is required (not `/`) because the repo's npm package.json lives in the wizard/ subdirectory. The repo root has no package.json.

    **Create `.github/workflows/dependabot-automerge.yml`:**

    ```yaml
    # Dependabot auto-merge workflow — SEC-01
    # Auto-merges patch-only Dependabot PRs once CI is green.
    # Minor and major updates require human review.
    # Uses pull_request (not pull_request_target) — Dependabot PRs have elevated
    # GITHUB_TOKEN permissions natively on pull_request events.
    name: Dependabot auto-merge

    on:
      pull_request:
        types: [opened, synchronize, reopened, ready_for_review]

    permissions:
      contents: write
      pull-requests: write

    jobs:
      automerge:
        runs-on: ubuntu-latest
        if: github.event.pull_request.user.login == 'dependabot[bot]'
        steps:
          - name: Fetch Dependabot metadata
            id: metadata
            uses: dependabot/fetch-metadata@v2
            with:
              github-token: ${{ secrets.GITHUB_TOKEN }}

          - name: Auto-merge patch updates only
            if: steps.metadata.outputs.update-type == 'version-update:semver-patch'
            run: gh pr merge --merge --auto "$PR_URL"
            env:
              PR_URL: ${{ github.event.pull_request.html_url }}
              GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    ```
  </action>
  <verify>
    <automated>test -f .github/dependabot.yml && test -f .github/workflows/dependabot-automerge.yml && grep -q "package-ecosystem: 'npm'" .github/dependabot.yml && grep -q "directory: '/wizard'" .github/dependabot.yml && grep -q "fetch-metadata" .github/workflows/dependabot-automerge.yml && echo "SEC-01 files OK"</automated>
  </verify>
  <done>
    - `.github/dependabot.yml` exists with npm ecosystem pointed at `/wizard` and github-actions at `/`, both weekly on Monday, both grouped by minor+patch
    - `.github/workflows/dependabot-automerge.yml` exists, fires on `pull_request`, guards on `dependabot[bot]`, auto-merges only when `update-type == version-update:semver-patch`
    - SEC-01 satisfied: template repo now ships Dependabot configuration with automerge for patches
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd wizard && npm test` — all existing tests pass (guard code does not break the test suite)
2. `cd wizard && npm run build` — produces `dist/index.cjs` cleanly
3. `test -f .github/dependabot.yml` — exits 0
4. `test -f .github/workflows/dependabot-automerge.yml` — exits 0
5. Node guard simulation: simulating Node 18 against `dist/index.cjs` prints the user-friendly error and exits 1
</verification>

<success_criteria>
- SEC-01 satisfied: `.github/dependabot.yml` committed with weekly npm (wizard/) + github-actions groups, automerge workflow in place for patches
- Node < 20 receives a clear error message (not a cryptic module crash) when attempting to run the wizard
- `wizard/package.json` has a `prepublishOnly` safety net that gates local `npm publish` behind tests + build
- Zero regressions: existing wizard tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-packaging-and-maintenance/05-01-SUMMARY.md`
</output>
