---
phase: 02-wizard-core
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified: []
autonomous: false
requirements: [WIZ-01, WIZ-02, WIZ-03, WIZ-04, WIZ-05, WIZ-06, WIZ-07, AI-01, AI-02, AI-03]

must_haves:
  truths:
    - "User sees @clack/prompts welcome banner when running the wizard"
    - "Prompt sequence presents AI methodology (radio), agentic system (radio), module multi-select with must-haves pre-checked"
    - "CTRL+C during prompts exits cleanly with a cancellation message — no stack trace"
    - ".template-config.json written after successful completion"
    - "./setup.sh --yes completes non-interactively and exits 0"
  artifacts:
    - path: ".template-config.json"
      provides: "Written config after wizard run"
  key_links:
    - from: "setup.sh"
      to: "wizard/dist/index.cjs"
      via: "node invocation with $@ passthrough"
---

<objective>
Human verification checkpoint: visually confirm the interactive wizard UI renders correctly, the prompt sequence is complete, CTRL+C exits cleanly, and setup.sh --yes works end-to-end.

Purpose: Unit tests cannot verify @clack/prompts ANSI rendering, TTY interaction, or the actual terminal UX. A single manual run confirms the full Phase 2 user-facing behaviour.
Output: Human approval that Phase 2 is shippable.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wizard-core/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete interactive wizard CLI for Phase 2:
- wizard/src/wizard.ts — runWizard() with @clack/prompts prompt sequence
- wizard/src/config.ts — readConfig / writeConfig / buildInitialConfig
- wizard/src/index.ts — CLI entry point with --yes flag and idempotency
- wizard/dist/index.cjs — built CJS binary
- setup.sh — repo-root entry point
- wizard/tests/wizard.test.ts + config.test.ts — full unit test coverage
  </what-built>
  <how-to-verify>
**Test 1: Interactive mode**
```bash
node wizard/dist/index.cjs
```
Expected:
- Welcome banner appears (create-ai-template header from @clack/prompts intro())
- Prompt 1: "Which AI methodology?" with 3 radio options (BMAD / GSD / Both)
- Prompt 2: "Which agentic system?" with 3 radio options (Claude Code / Cursor / VS Code)
- Prompt 3: "Select modules to install" multiselect — must-have modules (eslint, husky, vitest, tsconfig) are pre-checked
- After selecting and pressing Enter: outro message appears, wizard exits
- .template-config.json written at repo root with correct content

**Test 2: CTRL+C cancellation**
```bash
node wizard/dist/index.cjs
```
Press CTRL+C at any prompt. Expected: "Setup cancelled." message, wizard exits 0 (or non-zero) — NO stack trace or unhandled rejection.

**Test 3: --yes mode (non-interactive)**
```bash
./setup.sh --yes
```
Expected: Wizard runs without any interactive prompts, exits 0. Verify .template-config.json is written.
```bash
cat .template-config.json
```
Expected fields: wizardVersion, aiMethodology:'both', agenticSystem:'claude-code', modules array with pending entries.

**Test 4: Idempotency**
```bash
# Run --yes a second time immediately after Test 3
./setup.sh --yes
cat .template-config.json
```
Expected: modules list is NOT duplicated. Already-pending modules appear once.

**Cleanup after testing:**
```bash
rm -f .template-config.json
```

**Report any issues** with exact terminal output for debugging.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 tests pass, or describe the failure (which test, what you saw vs expected).</resume-signal>
</task>

</tasks>

<verification>
Human approval received.
</verification>

<success_criteria>
- Interactive wizard renders correctly in terminal
- All 4 manual tests pass
- No stack traces on CTRL+C
- .template-config.json is valid after --yes run
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-core/02-04-SUMMARY.md`
</output>
