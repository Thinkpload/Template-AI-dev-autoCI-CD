---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - wizard/templates/eslint/eslint.config.mjs
  - wizard/templates/husky/.husky/pre-commit
  - wizard/templates/husky/.husky/commit-msg
  - wizard/templates/husky/commitlint.config.mjs
  - wizard/templates/vitest/vitest.config.ts
  - wizard/templates/tsconfig/tsconfig.json
  - wizard/templates/bmad/.gitkeep
  - wizard/templates/gsd/.gitkeep
autonomous: true
requirements:
  - AI-04
  - QA-05

must_haves:
  truths:
    - "A template directory exists for each of the 6 registry modules"
    - "Template file contents use current API versions (Husky v9 no-shebang, ESLint v9 flat config, Vitest v4 coverage.include)"
    - "Husky hook files are stored with LF line endings in the Git index"
    - "vitest template outputs coverage/lcov.info matching sonar-project.properties"
  artifacts:
    - path: "wizard/templates/eslint/eslint.config.mjs"
      provides: "ESLint v9 flat config for target project"
    - path: "wizard/templates/husky/.husky/pre-commit"
      provides: "Husky v9 pre-commit hook (no shebang)"
    - path: "wizard/templates/husky/.husky/commit-msg"
      provides: "Husky v9 commit-msg hook (no shebang)"
    - path: "wizard/templates/husky/commitlint.config.mjs"
      provides: "commitlint conventional config"
    - path: "wizard/templates/vitest/vitest.config.ts"
      provides: "Vitest v4 config with lcov output and 80/70 thresholds"
    - path: "wizard/templates/tsconfig/tsconfig.json"
      provides: "TypeScript strict mode config with path aliases"
    - path: "wizard/templates/bmad/.gitkeep"
      provides: "Empty dir marker for bmad module (Phase 3 installer expects dir)"
    - path: "wizard/templates/gsd/.gitkeep"
      provides: "Empty dir marker for gsd module (Phase 3 installer expects dir)"
  key_links:
    - from: "wizard/templates/vitest/vitest.config.ts"
      to: "sonar-project.properties"
      via: "reportsDirectory: './coverage' + reporter: lcov → coverage/lcov.info"
      pattern: "coverage/lcov.info"
    - from: "wizard/templates/husky/.husky/pre-commit"
      to: ".gitattributes"
      via: "eol=lf rule prevents CRLF on Windows checkout"
      pattern: "*.sh text eol=lf"
---

<objective>
Create the `wizard/templates/` directory tree — one subdirectory per registry module containing pre-configured config files ready to be copied into target projects. Template files must match the API versions pinned in dependency-versions.ts (Husky v9 no-shebang, ESLint v9 flat config, Vitest v4 coverage API).

Purpose: Phase 3 installer does `cp wizard/templates/${moduleId}/* target-project/` — no path mapping needed. Templates must be correct at rest so installers stay simple.

Output: 8 template files across 6 module directories. Vitest template outputs `coverage/lcov.info` matching `sonar-project.properties`. Husky hook files stored as LF in Git index.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create code quality templates (eslint, husky, vitest, tsconfig)</name>
  <files>
    wizard/templates/eslint/eslint.config.mjs
    wizard/templates/husky/.husky/pre-commit
    wizard/templates/husky/.husky/commit-msg
    wizard/templates/husky/commitlint.config.mjs
    wizard/templates/vitest/vitest.config.ts
    wizard/templates/tsconfig/tsconfig.json
  </files>
  <action>
Create the following template files. These files will be copied verbatim into target projects by the Phase 3 installer — no variable substitution occurs in Phase 1 templates.

**wizard/templates/eslint/eslint.config.mjs** — ESLint v9 flat config (NOT .eslintrc.*):
```javascript
// Compatible with eslint ^9.19.0 and typescript-eslint ^8.23.0
// Review if major version bumps change flat config API
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommended,
);
```

**wizard/templates/husky/.husky/pre-commit** — Husky v9 format, NO shebang line:
```
npx lint-staged
```
This file must contain exactly one line: `npx lint-staged` with a trailing newline. No `#!/bin/sh`, no `#!/usr/bin/env sh` — Husky v9 hooks are plain script files, not shell scripts.

**wizard/templates/husky/.husky/commit-msg** — Husky v9 format, NO shebang line:
```
npx --no -- commitlint --edit $1
```
One line, trailing newline, no shebang.

**wizard/templates/husky/commitlint.config.mjs** — commitlint v19 conventional config:
```javascript
// Compatible with @commitlint/config-conventional ^19.6.0
export default {
  extends: ['@commitlint/config-conventional'],
};
```

**wizard/templates/vitest/vitest.config.ts** — Vitest v4 with coverage thresholds and lcov output:
```typescript
// Compatible with vitest ^4.0.0 and @vitest/coverage-v8 ^4.0.0
// Review coverage API on major version bumps
// Output path coverage/lcov.info matches sonar-project.properties sonar.javascript.lcov.reportPaths
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    coverage: {
      provider: 'v8',
      reporter: ['text', 'lcov'],
      reportsDirectory: './coverage',
      include: ['src/**/*.ts'],
      thresholds: {
        statements: 80,
        functions: 80,
        lines: 80,
        branches: 70,
      },
    },
  },
});
```

**wizard/templates/tsconfig/tsconfig.json** — TypeScript strict mode with path aliases:
```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "exactOptionalPropertyTypes": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

After creating the files, verify the Husky hook files have LF endings in the Git index:
```bash
git add wizard/templates/husky/
git ls-files --eol wizard/templates/husky/.husky/pre-commit
git ls-files --eol wizard/templates/husky/.husky/commit-msg
```
Expected output pattern: `i/lf` — the `i/` prefix indicates the index stores LF.
If `i/crlf` appears, the `.gitattributes` rule didn't apply — stop and investigate before continuing.
  </action>
  <verify>
    <automated>git ls-files --eol /c/Users/gllex/\!DEV_PROJECTS_2026/\!Template_BMAD+auto-CI-CD/wizard/templates/husky/.husky/pre-commit 2>/dev/null || echo "file not yet staged"</automated>
  </verify>
  <done>
    - All 6 template files exist in correct directories
    - `git ls-files --eol wizard/templates/husky/.husky/pre-commit` shows `i/lf` (or file is staged clean)
    - pre-commit and commit-msg files have no shebang line
    - vitest.config.ts uses `coverage.include` (not deprecated `coverage.all`) and `reportsDirectory: './coverage'`
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AI methodology template placeholders (bmad, gsd)</name>
  <files>
    wizard/templates/bmad/.gitkeep
    wizard/templates/gsd/.gitkeep
  </files>
  <action>
Create empty `.gitkeep` files in the bmad and gsd template directories. These modules have no static config files to copy in Phase 1 — their post-install scripts handle setup at install time. However, the Phase 3 installer will attempt to read `templates/${moduleId}/` and must find an existing directory.

**wizard/templates/bmad/.gitkeep** — empty file:
```
```
(zero bytes — just create an empty file)

**wizard/templates/gsd/.gitkeep** — empty file:
```
```
(zero bytes)

After creating the files, stage all template files and commit:
```bash
git add wizard/templates/
git commit -m "feat(01-03): add wizard template files for all 6 modules"
```

Verify the full template tree is correct:
```bash
find wizard/templates -type f | sort
```
Expected output:
```
wizard/templates/bmad/.gitkeep
wizard/templates/eslint/eslint.config.mjs
wizard/templates/gsd/.gitkeep
wizard/templates/husky/.husky/commit-msg
wizard/templates/husky/.husky/pre-commit
wizard/templates/husky/commitlint.config.mjs
wizard/templates/tsconfig/tsconfig.json
wizard/templates/vitest/vitest.config.ts
```

Also verify the husky hook line endings are correct in the committed state:
```bash
git ls-files --eol wizard/templates/husky/.husky/pre-commit
git ls-files --eol wizard/templates/husky/.husky/commit-msg
```
Both must show `i/lf`.
  </action>
  <verify>
    <automated>find /c/Users/gllex/\!DEV_PROJECTS_2026/\!Template_BMAD+auto-CI-CD/wizard/templates -type f | sort</automated>
  </verify>
  <done>
    - `wizard/templates/bmad/.gitkeep` and `wizard/templates/gsd/.gitkeep` exist
    - `find wizard/templates -type f | sort` shows all 8 expected files
    - Template commit exists in git log
    - `git ls-files --eol wizard/templates/husky/.husky/pre-commit` shows `i/lf`
  </done>
</task>

</tasks>

<verification>
1. `find wizard/templates -type f | sort` — shows 8 files across 6 module directories
2. `git ls-files --eol wizard/templates/husky/.husky/pre-commit` — shows `i/lf`
3. `grep -n "shebang\|#!/" wizard/templates/husky/.husky/pre-commit wizard/templates/husky/.husky/commit-msg` — no output (no shebang lines)
4. `grep "reportsDirectory" wizard/templates/vitest/vitest.config.ts` — shows `'./coverage'`
5. `grep "coverage/lcov.info" sonar-project.properties` — matches vitest template output path
</verification>

<success_criteria>
- All 6 module template directories exist: eslint, husky, vitest, tsconfig, bmad, gsd
- Husky hook files stored as LF in Git index (no CRLF contamination from Windows)
- No shebang lines in any Husky hook file
- vitest.config.ts uses Vitest v4 API (`coverage.include`, not `coverage.all`) and outputs to `./coverage` (lcov.info)
- Template files committed to Git
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
